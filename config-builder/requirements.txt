# Single source of truth for dependencies lives in pyproject.toml.
# This file is a thin pointer so `pip install -r requirements.txt` (used
# by deployment scripts and CI) keeps working.
#
# Runtime deps (PyYAML, pydantic) are declared in pyproject.toml under
# [project.dependencies] and pulled in transitively by `pip install -e .`.
-e .
